return {
  search_replace = {
    enabled = true,           -- Enable search & replace
    keybinding = "<leader>r"  -- Set custom keybinding for search & replace
  },
  multiline_cursors = {
    enabled = true,           -- Enable multiline cursors
    keybinding = "<C-S-M>"    -- Set custom keybinding for multiline cursors
  }
}

